//Triangle Area - js

// scripts.js

var base = 3,
	height = 4,
	traingleArea;

var base = prompt('Enter field base: only numbers (decimal separator - use dot)'),
	height = prompt('Enter height - only numbers (decimal separator - use dot)');

triangleArea = base * height / 2;

alert('Triangle field with base a: ' + base + ' and height h: ' + height + ' is equal to: ' + triangleArea);

console.log('Triangle field with base a: ' + base + ' and height h: ' + height + ' is equal to: ' + triangleArea);

